import { ReactNode } from 'react';

type Props = {
  children: ReactNode;
};

export const {{pascalCase componentName}} = ({ children }: Props) => {
  return (
    <div>
      <h1>{{pascalCase componentName}} Component</h1>
      {children}
    </div>
  );
};